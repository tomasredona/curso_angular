// import { createFeatureSelector, createSelector } from '@ngrx/store';

// export const selectAlumnos = createFeatureSelector<any[]>('alumnos');

// export const selectAlumnosList = createSelector(
//     selectAlumnos,
//     (state) => state
// );
